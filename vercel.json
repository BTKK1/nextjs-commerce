{
  "framework": "nextjs",
  "buildCommand": "pnpm run generate:demo-assets && pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "crons": [
    {
      "path": "/api/cron/catalog-reconcile",
      "schedule": "17 2 * * *"
    }
  ]
}
